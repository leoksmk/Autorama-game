# ORBITAL DERBY — dependências
#
# pygame-ce é o fork mantido da comunidade e o alvo recomendado; o pygame 2.x
# original também funciona (foi com ele que o projeto foi testado).
pygame-ce>=2.4

# OPCIONAL: só para os controles ESP32 pela USB (drivers/serial_driver.py).
# Sem ele o jogo avisa uma linha e roda no teclado, e a versão WebAssembly
# nunca chega a importá-lo — não existe porta COM no navegador.
pyserial>=3.5
